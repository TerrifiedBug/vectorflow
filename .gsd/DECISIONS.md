# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001/S04 | arch | Test framework for Next.js + tRPC + Prisma codebase | Vitest — to be set up in S04 | Standard choice for Next.js projects, fast, good TypeScript support, compatible with tRPC testing patterns | Yes — if Vitest proves incompatible with the codebase | agent |
| D002 | M001 | arch | Refactoring depth for baseline quality milestone | Moderate — split files over ~800 lines, extract duplicates, move inline logic to services; don't restructure entire module tree | User confirmed moderate approach — split worst offenders without deep restructuring. Keeps scope bounded. | No | collaborative |
| D003 | M001/S02 | convention | Whether purely declarative data files (vrl/function-registry.ts) count against the file size target | Exempt from ~800-line target | function-registry.ts is 1775 lines of structured data definitions, not logic. Splitting it would add indirection without improving maintainability. | Yes — if the file gains logic beyond data definitions | agent |
| D004 | M001/S02 | convention | Service extraction pattern for tRPC routers | Pure function exports, import prisma from @/lib/prisma, throw TRPCError for errors, accept Tx parameter for transaction-scoped work. Services must remain stateless — all singleton/side-effect access (metricStore, audit metadata) stays in the router. | Established during S02 pipeline and dashboard router extraction. Matches existing pipeline-version.ts pattern. Keeps services testable without mocking singletons. Router retains middleware chains (withAudit, withTeamAccess) and context-dependent operations. | Yes | agent |
| D005 | M001/S04 | testing | Prisma mocking strategy for unit tests | vi.mock('@/lib/prisma') with vitest-mock-extended's mockDeep<PrismaClient>() in a shared helper at src/__mocks__/lib/prisma.ts. Reset mock in beforeEach. Pass prismaMock as Tx for transaction-scoped functions. | Prisma-recommended approach for unit testing singleton pattern. Deep mock provides type-safe mockResolvedValue on any nested method. Avoids complexity of in-memory DB simulation (prismock, prisma-mock-vitest) which has uncertain Prisma 7 compatibility. The project uses a named export singleton (export const prisma) from @/lib/prisma importing PrismaClient from @/generated/prisma (custom output path). | Yes — if vitest-mock-extended proves incompatible with Prisma 7's generated client shape | agent |
